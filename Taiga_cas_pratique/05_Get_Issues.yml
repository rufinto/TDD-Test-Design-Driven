info:
  name: 05_Get_Issues
  type: http
  seq: 3

http:
  method: GET
  url: https://api.taiga.io/api/v1/issues?project={{project_id}}
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
  params:
    - name: project
      value: "{{project_id}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("La récupération des issues retourne 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
