info:
  name: 01_Login
  type: http
  seq: 1

http:
  method: POST
  url: https://api.taiga.io/api/v1/auth
  body:
    type: json
    data: |-
      {
        "type": "normal",
        "username": "abdelaziz.taiga",
        "password": "Taiga.2026"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        bru.setVar("auth_token", body.auth_token);
    - type: tests
      code: |-
        test("Le login retourne 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
