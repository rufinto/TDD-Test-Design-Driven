info:
  name: get status
  type: http
  seq: 7

http:
  method: GET
  url: https://api.taiga.io/api/v1/issue-statuses?project={{project_id}}
  params:
    - name: project
      value: "{{project_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
